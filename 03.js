const fn = () => 'cat'

const array = () => [];

const object = () => ({})
// since without the () it would look like in the case of declaring object, so it's important to put () in

const kansuu = () => () => {}